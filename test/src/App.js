import React from 'react'
import { createBrowserHistory } from 'history'

import './style.css'

const history = createBrowserHistory()

const App = () => {

    return (
        <div>heloo App!</div>
    )
}

export default App